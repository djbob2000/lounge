# Налаштування застосунку
PORT=3001
NODE_ENV=development

# Backblaze B2 налаштування
B2_APPLICATION_KEY_ID=your_application_key_id
B2_APPLICATION_KEY=your_application_key
B2_BUCKET_ID=your_bucket_id
B2_BUCKET_NAME=your_bucket_name

# --- Backblaze B2 Download URL ---
# Choose ONE of the following and ensure your bucket is configured accordingly.
# The B2_DOWNLOAD_URL should be the base URL. The file path (e.g., /file/your-bucket-name/image.jpg) will be appended by the application.

# Option 1: Using B2 Friendly URL (e.g., https://fXXX.backblazeb2.com)
# Replace f000 with your specific friendly URL prefix (e.g., f001, f002, f003, f004, f005).
# This is often simpler for direct linking and can be good for Next/Image if S3-style URLs cause issues.
# Example: B2_DOWNLOAD_URL=https://f003.backblazeb2.com
B2_DOWNLOAD_URL=https://f000.backblazeb2.com # Defaulting to f000 as a common example, adjust to your specific one

# Option 2: Using S3 Compatible URL (e.g., https://s3.region.backblazeb2.com)
# If you use this, the storage service might construct paths like: ${B2_DOWNLOAD_URL}/${B2_BUCKET_NAME}/path/to/file.jpg
# Ensure your application logic for constructing the final URL is compatible.
# Example: B2_DOWNLOAD_URL=https://s3.eu-central-003.backblazeb2.com

# Option 3: Using a custom domain mapped to your B2 bucket
# This requires additional setup in your DNS and B2 account.
# Example: B2_DOWNLOAD_URL=https://your-custom-domain.com


# Clerk налаштування
CLERK_SECRET_KEY=your_clerk_secret_key

# База даних
DATABASE_URL="postgresql://user:password@localhost:5432/lounge?schema=public" 